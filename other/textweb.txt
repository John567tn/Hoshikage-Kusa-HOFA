<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hoshikage Kusa | 星影茸社区门户</title>
    <!-- 引入样式和运行环境 -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400;500;700;900&display=swap');
        body { font-family: 'Noto Sans SC', sans-serif; }
        .animate-in { animation: fadeIn 0.4s ease-out forwards; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
        /* 隐藏未加载时的图标代码 */
        [data-lucide] { width: 20px; height: 20px; }
    </style>
</head>
<body class="bg-[#f8f9fa]">
    <div id="root"></div>

    <script type="text/babel">
        const { useState, useEffect } = React;

        // 图标组件：适配 Lucide CDN 版本
        const Icon = ({ name, size = 20, className = "" }) => {
            useEffect(() => {
                if (window.lucide) {
                    window.lucide.createIcons();
                }
            }, [name]);
            return <i data-lucide={name} style={{ width: size, height: size }} className={`inline-block ${className}`}></i>;
        };

        const App = () => {
            const [activeTab, setActiveTab] = useState('home');
            const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false);

            // 使用你上传的本地图片文件名
            const communityLogo = "12283.png"; 

            const communityInfo = {
                name: "Hoshikage Kusa • HOFA",
                tagline: "在这里，每一片星影都值得被温柔对待。"
            };

            const menuItems = [
                { id: 'home', label: '社区首页', icon: 'home' },
                { id: 'events', label: '兽聚资讯', icon: 'ticket' },
                { id: 'rules', label: '社区公约', icon: 'gavel' },
                { id: 'join', label: '加入社区', icon: 'user-plus' },
                { id: 'notices', label: '公告通知', icon: 'megaphone' },
                { id: 'gallery', label: '精彩相册', icon: 'image' },
            ];

            const groupBranches = [
                { id: 1, name: "星影茸 • HOFA 总群", type: "社区主群", description: "社区的主群，欢迎所有兽兽&飞友加入。", count: "10+ 成员", color: "from-emerald-500 to-teal-600" },
                { id: 2, name: "分支-福瑞(待创建)", type: "福瑞", description: "兽兽、Fursuiter 专属频道。", count: "0 成员", color: "from-blue-500 to-indigo-600" },
                { id: 3, name: "分支-飞友(待创建)", type: "飞友", description: "航空爱好者、模拟飞行玩家的集合点。", count: "0 成员", color: "from-sky-400 to-blue-500" }
            ];

            const events = [
                { id: 1, title: "上海兽聚 Test01", date: "待定", location: "上海", tag: "兽聚", image: "https://images.unsplash.com/photo-1540575467063-178a50c2df87?auto=format&fit=crop&w=800&q=80" },
                { id: 2, title: "模拟飞行联飞活动", date: "待定", location: "线上", tag: "飞友", image: "https://images.unsplash.com/photo-1492684223066-81342ee5ff30?auto=format&fit=crop&w=800&q=80" }
            ];

            const notices = [
                { id: 1, type: "必读", title: "【重要】群内昵称格式修改通知", date: "2025-12-16", content: "请务必按照格式修改：【福瑞/飞友】+ QQ名称。未修改者将无法获得分支群分配。" },
                { id: 2, type: "招募", title: "社区管理员招募中", date: "2024", content: "现需要招募两名常驻furry管理员以及三名非常驻管理员，详情请联系群主。" }
            ];

            return (
                <div className="flex min-h-screen bg-[#f8f9fa] text-slate-800">
                    {/* 侧边栏 */}
                    <aside className={`fixed inset-y-0 left-0 z-50 w-64 bg-slate-900 text-white transform transition-transform duration-300 ${isMobileMenuOpen ? 'translate-x-0' : '-translate-x-full'} md:translate-x-0`}>
                        <div className="flex flex-col h-full">
                            <div className="p-6 border-b border-slate-800 flex items-center gap-3">
                                <div className="w-10 h-10 bg-white rounded-lg overflow-hidden shadow-lg shrink-0">
                                    <img src={communityLogo} className="w-full h-full object-cover" onError={(e) => { e.target.src = "https://via.placeholder.com/40?text=H" }} />
                                </div>
                                <div>
                                    <h1 className="font-black text-lg">星影茸</h1>
                                    <p className="text-[10px] text-slate-400 uppercase tracking-widest">HOFA Community</p>
                                </div>
                            </div>
                            <nav className="flex-1 px-4 py-6 space-y-2">
                                {menuItems.map(item => (
                                    <button key={item.id} onClick={() => { setActiveTab(item.id); setIsMobileMenuOpen(false); }} className={`w-full flex items-center gap-3 px-4 py-3 rounded-xl transition-all ${activeTab === item.id ? 'bg-emerald-600 text-white shadow-lg' : 'text-slate-400 hover:bg-slate-800 hover:text-white'}`}>
                                        <Icon name={item.icon} />
                                        <span className="font-medium">{item.label}</span>
                                    </button>
                                ))}
                            </nav>
                            <div className="p-6 border-t border-slate-800 text-[10px] text-slate-500">
                                © 2024 Hoshikage Kusa
                            </div>
                        </div>
                    </aside>

                    {/* 内容 */}
                    <main className="flex-1 md:ml-64 min-h-screen flex flex-col">
                        <header className="md:hidden flex items-center justify-between p-4 bg-white border-b sticky top-0 z-40">
                            <div className="flex items-center gap-2">
                                <img src={communityLogo} className="w-8 h-8 rounded-md object-cover" />
                                <span className="font-bold text-sm">星影茸 • HOFA</span>
                            </div>
                            <button onClick={() => setIsMobileMenuOpen(true)} className="p-2"><Icon name="menu" /></button>
                        </header>

                        <div className="p-6 md:p-10 max-w-6xl w-full mx-auto">
                            {activeTab === 'home' && (
                                <div className="animate-in space-y-10">
                                    <div className="bg-white rounded-[2rem] p-8 md:p-16 relative overflow-hidden shadow-sm border border-slate-100">
                                        <div className="relative z-10">
                                            <span className="inline-block px-4 py-1 bg-emerald-100 text-emerald-700 rounded-full text-xs font-bold mb-4">Official Website</span>
                                            <h2 className="text-4xl md:text-5xl font-black mb-6 leading-tight">
                                                欢迎来到 <span className="text-emerald-600">星影茸</span> <br /> 社区门户。
                                            </h2>
                                            <p className="text-lg text-slate-500 max-w-xl">{communityInfo.tagline}</p>
                                        </div>
                                    </div>
                                    <div className="grid md:grid-cols-3 gap-6">
                                        <div className="bg-emerald-600 text-white p-8 rounded-[2rem] shadow-xl cursor-pointer" onClick={() => setActiveTab('events')}>
                                            <h3 className="text-xl font-bold mb-1">兽聚情报</h3>
                                            <p className="opacity-80 text-sm mb-6">规划你的面基之旅</p>
                                            <Icon name="arrow-right" />
                                        </div>
                                        <div className="bg-slate-900 text-white p-8 rounded-[2rem] shadow-xl cursor-pointer" onClick={() => setActiveTab('rules')}>
                                            <h3 className="text-xl font-bold mb-1">社区公约</h3>
                                            <p className="opacity-80 text-sm mb-6">共同维护社区环境</p>
                                            <Icon name="arrow-right" />
                                        </div>
                                        <div className="bg-white border p-8 rounded-[2rem] shadow-sm hover:border-emerald-500 transition-colors cursor-pointer" onClick={() => setActiveTab('join')}>
                                            <h3 className="text-xl font-bold mb-1">加入我们</h3>
                                            <p className="text-slate-500 text-sm mb-6">发现同好分支群</p>
                                            <Icon name="arrow-right" className="text-emerald-600" />
                                        </div>
                                    </div>
                                </div>
                            )}

                            {activeTab === 'join' && (
                                <div className="animate-in">
                                    <h2 className="text-3xl font-black mb-10">加入社区</h2>
                                    <div className="grid md:grid-cols-3 gap-6">
                                        {groupBranches.map(group => (
                                            <div key={group.id} className="bg-white p-8 rounded-[2rem] border border-slate-100 shadow-sm hover:shadow-xl transition-all">
                                                <div className={`w-12 h-12 rounded-xl bg-gradient-to-br ${group.color} text-white flex items-center justify-center mb-6`}>
                                                    <Icon name="users" />
                                                </div>
                                                <h3 className="text-xl font-bold mb-2">{group.name}</h3>
                                                <p className="text-slate-500 text-sm h-12 overflow-hidden mb-6">{group.description}</p>
                                                <div className="pt-4 border-t flex justify-between items-center text-xs text-slate-400">
                                                    <span>{group.count}</span>
                                                    <Icon name="chevron-right" className="text-emerald-600" />
                                                </div>
                                            </div>
                                        ))}
                                    </div>
                                </div>
                            )}

                            {activeTab === 'rules' && (
                                <div className="animate-in max-w-3xl">
                                    <h2 className="text-3xl font-black mb-8">社区规则</h2>
                                    <div className="space-y-4">
                                        <div className="bg-white p-6 rounded-2xl border flex gap-4">
                                            <div className="text-red-500 font-bold">2.1</div>
                                            <p className="text-sm text-slate-600">严禁讨论政治、敏感话题。本社区旨在提供轻松交流环境。</p>
                                        </div>
                                        <div className="bg-white p-6 rounded-2xl border flex gap-4">
                                            <div className="text-red-500 font-bold">2.2</div>
                                            <p className="text-sm text-slate-600">严禁发布 R18、过度擦边或让人不适的内容。</p>
                                        </div>
                                    </div>
                                </div>
                            )}

                            {activeTab === 'gallery' && (
                                <div className="animate-in">
                                    <h2 className="text-3xl font-black mb-8">社区相册</h2>
                                    <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
                                        {[1,2,3,4,5,6,7,8].map(i => (
                                            <div key={i} className="aspect-square rounded-2xl overflow-hidden bg-slate-200">
                                                <img src={`https://picsum.photos/seed/${i+10}/400`} className="w-full h-full object-cover hover:scale-110 transition duration-500" />
                                            </div>
                                        ))}
                                    </div>
                                </div>
                            )}
                            
                            {/* 公告板块 */}
                            {activeTab === 'notices' && (
                                <div className="animate-in space-y-4">
                                    <h2 className="text-3xl font-black mb-8">公告通知</h2>
                                    {notices.map(n => (
                                        <div key={n.id} className="bg-white p-6 rounded-2xl border shadow-sm">
                                            <div className="flex gap-2 mb-2">
                                                <span className="text-[10px] bg-emerald-600 text-white px-2 py-0.5 rounded uppercase font-bold">{n.type}</span>
                                                <span className="text-[10px] text-slate-400">{n.date}</span>
                                            </div>
                                            <h4 className="font-bold mb-2">{n.title}</h4>
                                            <p className="text-sm text-slate-500">{n.content}</p>
                                        </div>
                                    ))}
                                </div>
                            )}
                        </div>
                    </main>

                    {isMobileMenuOpen && <div className="fixed inset-0 bg-black/50 z-40 md:hidden" onClick={() => setIsMobileMenuOpen(false)} />}
                </div>
            );
        };

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<App />);
    </script>
</body>
</html>